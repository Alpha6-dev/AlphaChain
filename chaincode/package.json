{
  "name": "alphachain-chaincode",
  "version": "1.0.0",
  "description": "AlphaChain shipment lifecycle smart contract for Hyperledger Fabric",
  "main": "index.js",
  "scripts": {
    "start": "fabric-chaincode-node start",
    "test": "jest --coverage",
    "lint": "eslint ."
  },
  "dependencies": {
    "fabric-contract-api": "^2.5.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
